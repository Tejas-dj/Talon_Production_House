# R2 CDN — image delivery (see src/lib/r2.ts, src/lib/media/presets.ts).
# No env var needed: images are served from the fixed
# https://cdn.talonproductionhouse.com origin baked into src/lib/r2.ts.

# Bunny Stream — video delivery on the Volume network (see src/components/media/BunnyPlayer.tsx)
# The PULL ZONE hostname for your Stream library (not the numeric library id):
# Bunny dashboard -> Stream -> your library -> API -> "Pull Zone" / "CDN Hostname",
# looks like "vz-xxxxxxxx-yyy" (used as https://<value>.b-cdn.net/<videoId>/playlist.m3u8).
NEXT_PUBLIC_BUNNY_PULL_ZONE=

# Site URL — used to build absolute URLs in sitemap.ts / robots.ts.
# Leave unset for local dev (falls back to http://localhost:3000).
NEXT_PUBLIC_SITE_URL=

# Google Analytics 4 — measurement ID from GA4 admin -> Data Streams -> your stream.
# Leave unset to disable analytics (e.g. in local dev).
NEXT_PUBLIC_GA_MEASUREMENT_ID=

# Microsoft Clarity — project ID from the Clarity dashboard -> Settings -> Setup ->
# Install manually (the id after "/tag/" in the snippet's src, e.g. "yc4rh0aq55").
# Leave unset to disable session recording/heatmaps (e.g. in local dev).
NEXT_PUBLIC_CLARITY_PROJECT_ID=
